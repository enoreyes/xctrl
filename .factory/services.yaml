commands:
  build: cargo build
  test: cargo test -- --test-threads=2
  clippy: cargo clippy -- -D warnings
  fmt_check: cargo fmt --check
  fmt: cargo fmt
  lint: cargo clippy -- -D warnings && cargo fmt --check

services: {}
